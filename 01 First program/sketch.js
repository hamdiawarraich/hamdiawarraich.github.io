// First program
// Hamdia Warraich 
// 09-09-2025

function setup() { //rune ONCE at the start 
  createCanvas(windowWidth, windowHeight);
}

function draw() { //runs OVER and OVER 
                  //targeting 60 frames per second 
  //background(220);
  fill(255,0,0);
  circle(mouseX, mouseY, 30);
}
